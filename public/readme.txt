cambiar el idioma de un proyecto laravel:
creamos una carpeta es en resources/lang
creamos los mismos archivos que tenemos en la carpeta eng
y vamos a https://github.com/caouecs/Laravel-lang/tree/master/src/es 
y escribimos llenamos los archivos creados con el contenido que hay acá

creamos auth

creamos controladores

creamos modelos y migraciones, con schemma bilder

php artisan migrate

creamos otro controlador

las vistas: layouts, includes, páginas

seteamos el password en el modelo, para encriptar la contraseña

creamos request para crear y para actualizar

ingresamos las reglas para que sean validados